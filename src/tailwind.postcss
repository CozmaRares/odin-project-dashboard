@tailwind base;
@tailwind components;
@tailwind utilities;

@layer components {
  .tw-header-btn {
    @apply bg-sky-400 px-4 py-1 text-white rounded-[50rem] text-xl hover:bg-sky-600 transition-[background-color] duration-200 min-w-fit;
    width: calc((100% - 3rem) / 3);
  }
}
